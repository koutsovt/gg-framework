diff --git a/dist/index.es.js b/dist/index.es.js
index 5602e033a20be070dcae2d9d2c4f3109289fcc2f..ab46e533e7f3e1e1764d81e541d7065983a88295 100644
--- a/dist/index.es.js
+++ b/dist/index.es.js
@@ -2630,7 +2630,7 @@ function ns() {
     On = !0;
     return;
   }
-  const t = document.createElement("style");
+  const t = Object.assign(document.createElement("style"), { nonce: document.getElementById("app-style-nonce")?.nonce || "" });
   t.id = ea, t.textContent = es, document.head.appendChild(t), On = !0;
 }
 ns();
@@ -2715,7 +2715,8 @@ const no = Xa(function({
       K.current = Number.isFinite(z) ? z : 0;
     }
     const $ = () => {
-      const I = O.getBoundingClientRect(), z = Math.max(1, Math.round(I.width)), J = Math.max(1, Math.round(I.height));
+      // Canvas layers use local CSS pixels; viewport bounds include ancestor zoom/transforms.
+      const z = Math.max(1, O.offsetWidth), J = Math.max(1, O.offsetHeight);
       return { cssWidth: z, cssHeight: J, cornerRadius: Y(z, J) };
     }, W = $();
     F.current = yr({
@@ -2934,7 +2935,7 @@ function Is({
   _t(() => {
     var b, w, M, y;
     if (!document.getElementById(na)) {
-      const C = document.createElement("style");
+      const C = Object.assign(document.createElement("style"), { nonce: document.getElementById("app-style-nonce")?.nonce || "" });
       C.id = na, C.textContent = ds, document.head.appendChild(C);
     }
     (b = h.current) == null || b.setAttribute("data-mfx-bare", "");
@@ -3122,7 +3123,7 @@ function Bs(t, e = () => ao) {
       Z(j, R, Y), tt.x = j + Y.x, tt.y = R + Y.y;
     }, L = "mfx-bend-style";
     if (!document.getElementById(L)) {
-      const j = document.createElement("style");
+      const j = Object.assign(document.createElement("style"), { nonce: document.getElementById("app-style-nonce")?.nonce || "" });
       j.id = L, j.textContent = ".metal-fx-root[data-mfx-bend]::before,.metal-fx-root[data-mfx-bend]::after{box-shadow:none!important}", document.head.appendChild(j);
     }
     let O = !1, D = null;
